Better Auth client adapter modules.

Current:

- Better Auth client creation
- bridge-state creation helpers
- runtime snapshot derivation helpers
- session restore helper surfaces

Use these modules to bridge Better Auth into `convex-auth/react` runtime semantics.
Do not put app-specific permission logic here.
