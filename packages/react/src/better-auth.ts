/**
 * Legacy Better Auth compatibility surface.
 *
 * Consumers migrating from the Better Auth runtime can import from
 * `convex-auth-react/better-auth` while they switch over to the native
 * `convex-auth-react` (or `convex-auth-react/client`) entry.
 */
export * from "./better-auth-runtime";
export * from "./better-auth-app-runtime";
export * from "./BetterAuthConvexProvider";
