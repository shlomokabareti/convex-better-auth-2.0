feat(react-native): add accept-invite and post-sign-up pages

Extend auth-pages.tsx with:
- ExpoAuthPostSignUpPage — finalizes workspace access using getDefaultOrganization,
  getAvailableOrganizations, ensureActiveOrganization, and optional redeemInvitation.
- ExpoAuthAcceptInvitePage — looks up invite by token and navigates to sign-up.

Wire both into createExpoConvexAuthRuntime as AuthPostSignUpRoutePage and
AuthAcceptInviteRoutePage.

Proof: workspace typecheck, check, build, 1068 tests pass.
