build(react): drop convex-better-auth from react and react-native deps

- Remove the convex-better-auth workspace package from dependencies and
  peerDependencies of convex-auth-react and convex-auth-react-native.
- Make better-auth an optional peer in convex-auth-react-native so the
  default entry does not require it.
- Keep convex-better-auth-adapter as a dev/optional peer for the
  better-auth compat subpath only.

Proof: workspace typecheck, lint, and 1068 tests green.
