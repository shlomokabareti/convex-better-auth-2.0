# `examples/oauth`

This is a React consumer that only demonstrates Google, GitHub, and Discord OAuth. It is intended to be dropped into a Vite + React app. For a fully configured runnable project, see `examples/react` and add the OAuth buttons from `src/App.tsx`.
