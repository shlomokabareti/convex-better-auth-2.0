# `examples/react-native`

This is a minimal Expo / React Native consumer. The complete configuration lives in `docs/react-native.md`; this directory only contains the `App.tsx` entry point.

To make it runnable, create an Expo project with `npx create-expo-app`, add `convex-auth-react-native`, and drop `src/App.tsx` into your app root.
